#version 410

layout (location  = 0) in vec3 vertPos; 

out vec3 fragPos;

uniform mat4 camera;

void main()
{
	gl_Position = (camera * vec4(vertPos, 1.0));
	gl_Position.w=1.0;
	
	fragPos = vertPos;	
}