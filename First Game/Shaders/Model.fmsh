#version 410

in vec2 fragUV;

out vec4 colour;

//uniform float tm;
//uniform sampler2D texSampler;

uniform vec4 colourMod;

uniform sampler2D diffuseTexture;
uniform sampler2D specularTexture;

void main()
{

	//colour  = mix(texture(diffuseTexture,fragUV),
	//			  texture(specularTexture,fragUV),
	//			  0.9);
	
    //colour = colourMod;
	colour  = texture(diffuseTexture,fragUV); 

	//colour = texture(specularTexture,fragUV);
 
	//colour = colourMod;
}